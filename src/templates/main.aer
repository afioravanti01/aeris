/// Hello, Aeris.
///
/// `cap` here is the synthesised capability `main` receives from
/// `aeris.toml [caps]` (§ 8.4). `io.println` is diagnostic-class
/// — it bypasses the V2 mandatory-intent rule.
fn main(cap) -> result<unit> {
  io.println("hello, aeris")
  Ok(())
}
