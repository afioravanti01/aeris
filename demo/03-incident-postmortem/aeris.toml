[project]
name  = "incident-postmortem"
aeris = "0.3.0"

[caps]
# Script mode — trace, replay, model@vN validation and intent
# blocks are still active.
enforce = "off"

[ai.backend]
kind = "cli"
cmd  = "claude --print --dangerously-skip-permissions --no-session-persistence --effort low --output-format text --model claude-sonnet-4-6"
