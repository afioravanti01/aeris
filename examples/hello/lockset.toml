[project]
name  = "hello"
aeris = "0.2.0"

[deps]

[caps]
# Prototype mode (§ 8.4.1): functions may call capability operations
# without declaring an enclosing `cap` parameter. `io.*` is
# diagnostic-class and requires no allow-list dimension.
required = false
